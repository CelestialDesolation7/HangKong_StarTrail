<Window x:Class="HangKong_StarTrail.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HangKong_StarTrail"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        xmlns:fontAwesome="clr-namespace:FontAwesome.WPF;assembly=FontAwesome.WPF"
        mc:Ignorable="d"
        Title="星穹轨道 —— 恒星运行与宇宙知识可视化系统" 
        Height="720" Width="1280" 
        WindowStartupLocation="CenterScreen" 
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4169E1"/>
            <Setter Property="Foreground" Value="#F0F0F0"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="20"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFD700"/>
                                <Setter Property="Foreground" Value="#0B0E17"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavigationButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4169E1"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1E90FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="#0B0E17">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Grid Grid.Row="0" Background="#0F1525" MouseDown="TitleBar_MouseDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="星穹轨道" Foreground="#F0F0F0" FontSize="14" Margin="10,5,0,0" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" Click="MinimizeButton_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0">
                        <fontAwesome:FontAwesome Icon="WindowMinimize" Foreground="#F0F0F0" FontSize="14"/>
                    </Button>
                    <Button x:Name="CloseButton" Click="CloseButton_Click" Width="30" Height="30" Background="Transparent" BorderThickness="0">
                        <fontAwesome:FontAwesome Icon="Close" Foreground="#F0F0F0" FontSize="14"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- 主内容区 -->
            <Grid Grid.Row="1">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#0B0E17" Offset="0"/>
                        <GradientStop Color="#191E2E" Offset="0.5"/>
                        <GradientStop Color="#0B0E17" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                
                <!-- 星星效果 - 使用Canvas和Ellipse模拟 -->
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Canvas>
                    <!-- 小星星点缀 -->
                    <Ellipse Canvas.Left="100" Canvas.Top="50" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.8"/>
                    <Ellipse Canvas.Left="200" Canvas.Top="150" Width="1" Height="1" Fill="#FFFFFF" Opacity="0.7"/>
                    <Ellipse Canvas.Left="300" Canvas.Top="80" Width="3" Height="3" Fill="#FFFFFF" Opacity="0.9"/>
                    <Ellipse Canvas.Left="500" Canvas.Top="200" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.8"/>
                    <Ellipse Canvas.Left="700" Canvas.Top="100" Width="1" Height="1" Fill="#FFFFFF" Opacity="0.7"/>
                    <Ellipse Canvas.Left="900" Canvas.Top="250" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.8"/>
                    <Ellipse Canvas.Left="1100" Canvas.Top="80" Width="3" Height="3" Fill="#FFFFFF" Opacity="0.9"/>
                    <Ellipse Canvas.Left="150" Canvas.Top="300" Width="1" Height="1" Fill="#FFFFFF" Opacity="0.6"/>
                    <Ellipse Canvas.Left="350" Canvas.Top="350" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.8"/>
                    <Ellipse Canvas.Left="550" Canvas.Top="400" Width="1" Height="1" Fill="#FFFFFF" Opacity="0.7"/>
                    <Ellipse Canvas.Left="750" Canvas.Top="300" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.9"/>
                    <Ellipse Canvas.Left="950" Canvas.Top="450" Width="1" Height="1" Fill="#FFFFFF" Opacity="0.7"/>
                    <Ellipse Canvas.Left="1050" Canvas.Top="350" Width="2" Height="2" Fill="#FFFFFF" Opacity="0.8"/>
                    
                    <!-- 星云效果 - 使用半透明椭圆 -->
                    <Ellipse Canvas.Left="200" Canvas.Top="150" Width="400" Height="200" Opacity="0.1">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#4169E1" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    
                    <Ellipse Canvas.Left="600" Canvas.Top="250" Width="500" Height="300" Opacity="0.15">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="#FF4500" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                </Canvas>

                <!-- 标题和介绍 -->
                <StackPanel Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Center">
                    <TextBlock Text="星穹轨道" FontFamily="Microsoft YaHei" FontSize="60" Foreground="#FFD700" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,10">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="3" Direction="315" Color="#000000" BlurRadius="4" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="恒星运行与宇宙知识可视化系统" FontFamily="Microsoft YaHei" FontSize="24" Foreground="#F0F0F0" TextAlignment="Center" Margin="0,0,0,20">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="2" Direction="315" Color="#000000" BlurRadius="3" Opacity="0.7"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <!-- 按钮区域 -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="启动探索之旅" Style="{StaticResource MainButtonStyle}" Width="250" Click="StartExploration_Click"/>
                    <Button Content="载入已有场景" Style="{StaticResource MainButtonStyle}" Width="250" Click="LoadScene_Click"/>
                    <Button Content="学习宇宙知识" Style="{StaticResource MainButtonStyle}" Width="250" Click="StartLearning_Click"/>
                    <Button Content="系统设置" Style="{StaticResource MainButtonStyle}" Width="250" Click="OpenSettings_Click"/>
                    <Button Content="退出系统" Style="{StaticResource MainButtonStyle}" Width="250" Click="Exit_Click"/>
                </StackPanel>

                <!-- 底部版权信息 -->
                <StackPanel Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock Text="© 2025 星穹轨道团队 版本 1.0.0" Foreground="#A0A0A0" FontSize="12" TextAlignment="Center"/>
                    <TextBlock Text="软件构造课程项目" Foreground="#A0A0A0" FontSize="12" TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
